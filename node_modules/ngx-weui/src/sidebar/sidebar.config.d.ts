export declare type PositionType = 'left' | 'right' | 'top' | 'bottom';
export declare type ModeType = 'over' | 'slide';
export declare class SidebarConfig {
    /**
     * 位置方向，默认：`left`
     */
    position: PositionType;
    /**
     * 类型，默认：`slide`
     * - over: 不覆盖
     * - slide：侧边移动
     */
    mode: ModeType;
    /**
     * 允许点击背景关闭，默认：`true`
     */
    backdrop: boolean;
}
