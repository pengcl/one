export declare class PTRConfig {
    /**
     * 是否使用默认icon样式，默认：`false`
     */
    customIcon?: boolean;
    /**
     * 下拉icon，支持HTML
     */
    pullIcon?: string;
    /**
     * 加载中icon，支持HTML
     */
    loadingIcon?: string;
    /**
     * 加载成功icon，支持HTML
     */
    successIcon?: string;
    /**
     * 下拉刷新容器高度（单位：px），默认：`100`
     */
    height?: number;
    /**
     * 下拉范围有效（单位：%），默认：`80`
     */
    treshold?: number;
}
