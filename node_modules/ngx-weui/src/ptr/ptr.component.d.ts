import { OnChanges, SimpleChanges, EventEmitter, ElementRef, OnInit } from '@angular/core';
import { PTRConfig } from './ptr.config';
export declare class PTRComponent implements OnInit, OnChanges {
    private el;
    private DEF;
    private ogY;
    private loading;
    private touching;
    private touchId;
    _animating: boolean;
    private initScrollTop;
    _pullPercent: number;
    private loaderEl;
    private iconEl;
    private contentEl;
    _lastLabel: string;
    /** 配置项 */
    config: PTRConfig;
    /** 是否禁止 */
    disabled: boolean;
    /** 下拉滚动时回调，返回一个0-100%的参数 */
    scroll: EventEmitter<number>;
    /** 刷新回调 */
    refresh: EventEmitter<PTRComponent>;
    constructor(el: ElementRef, DEF: PTRConfig);
    /**
     * 设置最后更新标签
     *
     * @param label 标签内容（支持HTML）
     */
    setLastUpdatedLabel(label: string): void;
    /**
     * 设置刷新成功
     *
     * @param [lastUpdatedLabel] label 标签内容（支持HTML）
     */
    setFinished(lastUpdatedLabel?: string): void;
    onTouchStart($event: any): void;
    onTouchMove($event: any): void;
    onTouchEnd($event: any): void;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private parseConfig();
}
