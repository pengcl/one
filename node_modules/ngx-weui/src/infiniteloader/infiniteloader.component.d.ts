import { OnChanges, SimpleChanges, EventEmitter, ElementRef, OnDestroy, NgZone, OnInit } from '@angular/core';
import { InfiniteLoaderConfig } from './infiniteloader.config';
export declare class InfiniteLoaderComponent implements OnChanges, OnInit, OnDestroy {
    private el;
    private zone;
    private DEF;
    private didScroll;
    private scrollEvent;
    private scrollTime;
    private disposeScroller;
    _loading: boolean;
    _finished: boolean;
    /**
     * 配置项
     */
    config: InfiniteLoaderConfig;
    /**
     * 加载更多回调
     */
    loadmore: EventEmitter<InfiniteLoaderComponent>;
    constructor(el: ElementRef, zone: NgZone, DEF: InfiniteLoaderConfig);
    /** 设置本次加载完成 */
    resolveLoading(): void;
    /** 设置结束 */
    setFinished(): void;
    /** 设置重新开始 */
    restart(): void;
    _onScroll(): void;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private parseConfig();
}
