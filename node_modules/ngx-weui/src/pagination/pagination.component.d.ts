import { EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { PaginationMode } from './pagination.type';
import { PaginationConfig } from './pagination.config';
export declare class PaginationComponent implements OnChanges {
    _ptArr: number[];
    /**
     * 形态，可选 `button`,`pointer`，默认：`button`
     */
    mode: PaginationMode;
    /** 当前索引 */
    current: number;
    /** 数据总数 */
    total: number;
    /**
     * 是否隐藏数值，默认：`false`
     */
    simple: boolean;
    /**
     * 小号按钮，默认：`true`
     */
    mini: boolean;
    /**
     * 上一页文本（支持HTML），默认：`上一页`
     */
    prevText: string;
    /**
     * 下一页文本（支持HTML），默认：`下一步`
     */
    nextText: string;
    /** 分页触发的回调函数 */
    change: EventEmitter<number>;
    constructor(cog: PaginationConfig);
    ngOnChanges(changes: SimpleChanges): void;
    _prevDisabled: boolean;
    _nextDisabled: boolean;
    _checkDisabled(): void;
    _goto(value: number): boolean;
}
