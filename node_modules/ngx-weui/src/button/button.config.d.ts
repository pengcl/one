import { ButtonType } from '../utils/types';
export declare class ButtonConfig {
    /**
     * 操作场景：primary、default、warn
     */
    type: ButtonType;
    /**
     * disabled状态，默认：`false`
     */
    disabled: boolean;
    /**
     * 是否加载状态，默认：`false`
     */
    loading: boolean;
    /**
     * 镂空按钮，默认：`false`
     */
    plain: boolean;
    /**
     * 是否小号，默认：`false`
     */
    mini: boolean;
}
