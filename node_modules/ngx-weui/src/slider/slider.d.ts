import { ElementRef, OnDestroy, EventEmitter, OnChanges, SimpleChanges, OnInit } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
/**
 * 滑块指令，支持[(ngModel)]
 */
export declare class SliderDirective implements ControlValueAccessor, OnInit, OnDestroy, OnChanges {
    private el;
    private _state;
    private _value;
    private isInit;
    private trackEl;
    private handlerEl;
    onTouchStart: any;
    onTouchMove: any;
    /**
     * 允许的最小值，默认：`0`
     */
    min: number;
    /**
     * 允许的最大值，默认：`100`
     */
    max: number;
    /**
     * 步长，默认：`1`
     */
    step: number;
    /**
     * 是否可用
     */
    enabled: boolean;
    /**
     * 值改变时触发
     */
    change: EventEmitter<number>;
    constructor(el: ElementRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private refresh();
    private setValue(value);
    private layout();
    private startHandle($event);
    private moveHandle($event);
    private getPercentage(pageX, $event);
    private calculateValue(percentage);
    ngOnChanges(changes: SimpleChanges): void;
    writeValue(value: any): void;
    private onChange;
    private onTouched;
    registerOnChange(fn: (_: any) => {}): void;
    registerOnTouched(fn: () => {}): void;
    setDisabledState(isDisabled: boolean): void;
}
