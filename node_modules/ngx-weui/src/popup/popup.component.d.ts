import { OnDestroy, EventEmitter, OnChanges, SimpleChanges, OnInit } from '@angular/core';
import { Observable } from 'rxjs';
import { PopupConfig } from './popup.config';
export declare class PopupComponent implements OnInit, OnDestroy, OnChanges {
    private DEF;
    /**
     * 配置项
     */
    config: PopupConfig;
    /** 取消回调 */
    cancel: EventEmitter<{}>;
    /** 确认回调 */
    confirm: EventEmitter<{}>;
    private shown;
    _shownAnt: boolean;
    private observer;
    readonly _visibility: string;
    constructor(DEF: PopupConfig);
    private parseConfig();
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    /**
     * 显示，并支持订阅结果，如果点击取消值为false，反之 true
     */
    show(): Observable<boolean>;
    /**
     * 隐藏
     *
     * @param [is_backdrop] 是否从背景上点击(可选)
     */
    hide(is_backdrop?: boolean): boolean;
    /** 关闭，等同 `hide()` 效果 */
    close(): void;
    _onCancel(): boolean;
    _onConfirm(): boolean;
    ngOnDestroy(): void;
}
