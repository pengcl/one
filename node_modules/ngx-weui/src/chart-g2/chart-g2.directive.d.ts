import { OnInit, OnDestroy, ElementRef, OnChanges, SimpleChanges, NgZone } from '@angular/core';
export declare class ChartG2Directive implements OnInit, OnDestroy, OnChanges {
    private el;
    private zone;
    _chart: any;
    /**
     * chart实例对象
     */
    readonly chart: any;
    /**
     * GM对象
     */
    readonly GM: any;
    /**
     * 画布内部的边距，可以是数组 [top, right, bottom, left] 也可以是一个数字。
     */
    margin: number[] | number;
    private initFlag;
    constructor(el: ElementRef, zone: NgZone);
    ngOnInit(): void;
    private buildChart();
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
}
