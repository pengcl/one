import { OnChanges, SimpleChanges, ElementRef, OnInit } from '@angular/core';
/**
 * 文本域字数统计
 */
export declare class TextareaDirective implements OnInit, OnChanges {
    private el;
    /**
     * 最大长度，0表示不受限
     */
    maxlength: number;
    /**
     * 中文部分应该算多少个字符，使用 `/[^\x00-\xff]/g` 正则表达式统计中文部分（默认：1个字符）
     */
    cn: number;
    private _value;
    private _count;
    constructor(el: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private init();
    _onChange(value: any): void;
}
