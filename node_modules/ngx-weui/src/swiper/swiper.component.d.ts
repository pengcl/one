import { OnChanges, SimpleChanges, OnDestroy, ElementRef, NgZone, OnInit, AfterViewInit } from '@angular/core';
import { SwiperConfig } from './swiper.config';
export declare class SwiperComponent implements AfterViewInit, OnChanges, OnInit, OnDestroy {
    private el;
    private zone;
    private DEF;
    /**
     * 等同于swiper[参数项](http://idangero.us/swiper/api/)
     */
    options: any;
    constructor(el: ElementRef, zone: NgZone, DEF: SwiperConfig);
    /**
     * Swiper 实例对象，可以通过 `@ViewChild` 访问到它
     */
    swiper: any;
    private initOptions();
    private containerEl;
    private init();
    private destroy();
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
}
