import { ComponentFactoryResolver, ApplicationRef, Injector } from '@angular/core';
import { Observable } from 'rxjs';
import { ActionSheetConfig } from './actionsheet.config';
import { BaseService } from '../utils/base.service';
export declare class ActionSheetService extends BaseService {
    constructor(resolver: ComponentFactoryResolver, applicationRef: ApplicationRef, injector: Injector);
    /**
     * 创建一个弹出式菜单并显示
     *
     * @param menus 菜单内容
     * @param config 配置性（可选）
     * @returns 可订阅来获取结果
     */
    show(menus: {
        text?: string;
        [key: string]: any;
    }[], config?: ActionSheetConfig): Observable<any>;
}
