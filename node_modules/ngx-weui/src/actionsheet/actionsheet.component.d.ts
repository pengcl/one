import { OnDestroy, EventEmitter } from '@angular/core';
import { ActionSheetConfig } from './actionsheet.config';
import { Observable } from 'rxjs';
export declare class ActionSheetComponent implements OnDestroy {
    private DEF;
    /**
     * 配置项
     */
    config: ActionSheetConfig;
    /**
     * 菜单内容
     */
    menus: {
        text?: string;
        [key: string]: any;
    }[];
    /**
     * 关闭回调
     */
    close: EventEmitter<{}>;
    _shown: boolean;
    /**
     * 动画状态码
     */
    _shownAnt: boolean;
    private observer;
    readonly _visibility: string;
    constructor(DEF: ActionSheetConfig);
    /**
     * 显示，组件载入页面后并不会显示，显示调用 `show()` 并订阅结果。
     */
    show(): Observable<any>;
    /**
     * 隐藏
     *
     * @param is_backdrop 是否从背景上点击
     */
    hide(is_backdrop?: boolean): boolean;
    /**
     * 选择动作
     */
    _onSelect(menu: {
        text?: string;
        [key: string]: any;
    }): void;
    ngOnDestroy(): void;
}
