import { EventEmitter, SimpleChanges, OnChanges } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { RatingConfig } from './rating.config';
export declare class RatingComponent implements ControlValueAccessor, OnChanges {
    private DEF;
    /** 配置项 */
    config: RatingConfig;
    /** 是否只读模式，默认：`false` */
    readonly: boolean;
    /** 选中后回调，参数：选中值 */
    selected: EventEmitter<number>;
    _range: any[];
    _value: number;
    _preValue: number;
    _class: string;
    constructor(DEF: RatingConfig);
    _setConfig(cog: RatingConfig): void;
    ngOnChanges(changes: SimpleChanges): void;
    _rate(value: number): void;
    writeValue(_value: any): void;
    private onChange;
    private onTouched;
    registerOnChange(fn: (_: any) => {}): void;
    registerOnTouched(fn: () => {}): void;
    setDisabledState(isDisabled: boolean): void;
}
