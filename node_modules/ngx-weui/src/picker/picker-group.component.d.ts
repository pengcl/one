import { EventEmitter, OnDestroy, OnChanges, SimpleChanges } from '@angular/core';
import { PickerData } from './data';
/**
 * 多列选择器组
 */
export declare class PickerGroupComponent implements OnDestroy, OnChanges {
    /** 数据列表 */
    items: PickerData[];
    /** 当前默认位置 */
    defaultIndex: number;
    /** 多列中的位置 */
    groupIndex: number;
    /** 变更回调 */
    change: EventEmitter<any>;
    private defaults;
    private startTime;
    private startY;
    private endY;
    private speed;
    _animating: boolean;
    _distance: number;
    ngOnChanges(changes: SimpleChanges): void;
    onTouchStart(e: TouchEvent): void;
    onTouchMove(e: TouchEvent): void;
    onTouchEnd(event: TouchEvent): void;
    private stop(diff);
    private onChange(item, index?);
    private _getMax(offset, rowHeight);
    private _getMin(offset, rowHeight, length);
    ngOnDestroy(): void;
}
